# API
PORT=5001
JWT_SECRET=dev-secret-change-me
ALLOWED_ORIGIN=http://localhost:5173

# DB: sqlite3 (default, zero infra) | pg (requer docker-compose up -d db)
DB_CLIENT=sqlite3
DATABASE_FILE=./make-it.sqlite
DATABASE_URL=postgres://makeit:makeit@localhost:5432/makeit

# Web (Vite) — precisa do prefixo VITE_
VITE_API_URL=http://localhost:5001/api/v1
